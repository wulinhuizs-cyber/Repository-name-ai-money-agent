name: Daily Simple Report

on:
  workflow_dispatch:
  schedule:
    - cron: '0 0 * * *'

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - name: Fetch News
        run: |
          echo "=== 今日信息源 ==="
          curl -s https://rsshub.app/github/trending/daily | head -n 20
          echo ""
          echo "=== YouTube AI频道 ==="
          curl -s https://rsshub.app/youtube/channel/UCiT9RITQ9PW6BhXK0y2jaeg | head -n 20
          
